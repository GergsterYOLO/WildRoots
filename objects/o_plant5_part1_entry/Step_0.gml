x = camera_get_view_x(view_camera[0]) + x_offset;

if (global.nbvisible) {
	var mountain_map = global.notebook[? "Mountain"];
	if (mountain_map[? "aspects_discovered"][0] && global.pagenum == 5) {
		visible = true;
    }

} else {
	visible = false;
}