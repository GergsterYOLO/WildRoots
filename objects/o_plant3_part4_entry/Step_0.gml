x = camera_get_view_x(view_camera[0]) + x_offset;

if (global.nbvisible) {
	var palmers_map = global.notebook[? "Palmers"];
	if (palmers_map[? "aspects_discovered"][3] && global.pagenum == 3) { //3
		visible = true;
    }

} else {
	visible = false;
}