x = camera_get_view_x(view_camera[0]) + x_offset;

if (global.nbvisible) {
	var globemallow_map = global.notebook[? "Globemallow"];
	if (globemallow_map[? "aspects_discovered"][3] && global.pagenum == 2) { //4
		visible = true;
    }

} else {
	visible = false;
}