x = camera_get_view_x(view_camera[0]) + x_offset;

if (global.nbvisible) {
	var fremont_map = global.notebook[? "Fremont"];
	if (fremont_map[? "aspects_discovered"][1] && global.pagenum == 4) { //4
		visible = true;
    }

} else {
	visible = false;
}